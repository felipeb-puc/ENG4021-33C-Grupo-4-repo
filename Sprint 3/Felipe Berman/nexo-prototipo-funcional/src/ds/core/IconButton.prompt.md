Circular icon-only button for toolbars, carousel arrows, social links and card affordances.

```jsx
<IconButton name="bookmark" label="Salvar vaga" />
<IconButton name="arrow-right" variant="primary" size="lg" label="Próximo" />
```

Always circular (`--radius-pill`). Use `inverse` on dark green bands.
