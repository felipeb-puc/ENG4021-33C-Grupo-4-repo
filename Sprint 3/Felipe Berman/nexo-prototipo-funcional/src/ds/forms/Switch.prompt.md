42×24 track toggle for instant-effect settings (alerts, visibility). For form values that need a submit, use Checkbox.

```jsx
<Switch label="Alerta de novas vagas" checked={on} onChange={e => setOn(e.target.checked)} />
```
