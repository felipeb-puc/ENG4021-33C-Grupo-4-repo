Labelled text field — 8px radius (fields are the one place the system is NOT pill-shaped, except search).

```jsx
<Input label="E-mail institucional" placeholder="voce@universidade.br" icon="mail" />
<Input label="CPF" error="CPF inválido" />
```
